import Head from "next/head";
import React from "react";
import Layout from "../components/Layout";

const Uses = () => {
  return (
    <Layout>
      <Head>
        <title>Uses | Kai Chi</title>
      </Head>
    </Layout>
  );
};

export default Uses;
